# Repository
```
bedrock-chat-app/
│── .streamlit/
│   └── secrets.toml         ✅ (add AWS keys here for local testing)
│── app.py                   ✅ (chat UI)
│── bedrock_client.py        ✅ (Bedrock setup)
│── requirements.txt         ✅ (dependencies)
│── README.md                ✅ (optional)
│── .gitignore               ✅ (prevent secrets from being pushed)
```